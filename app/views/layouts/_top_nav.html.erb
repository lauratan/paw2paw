<nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#custom-collapse"><span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button><a class="navbar-brand" href="/">Paw 2 Paw</a>
    </div>
    <div class="collapse navbar-collapse" id="custom-collapse">
      <ul class="nav navbar-nav navbar-right">
      <li class="dropdown"><%= link_to '<i class="fa fa-search" aria-hidden="true"></i>Search'.html_safe, sitters_path %></li>
      <% if current_user && !current_sitter %>
      <li class="dropdown"><%= link_to '<i class="fa fa-paw" aria-hidden="true"></i>Become A Sitter'.html_safe, new_sitter_path %></li>
      <li class="dropdown"><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
      <li class="dropdown"><%= link_to current_user.first_name, user_bookings_index_path(current_user.id)%></li>
      <span><%= link_to image_tag(current_user.avatar, class:"navbar-avatar"), user_bookings_index_path(current_user.id)%></span>
      <% elsif current_sitter %>
        <li class="dropdown">
            <a class="dropdown-toggle" href="#" data-toggle="dropdown">Sitter Menu</a>
          <ul class="dropdown-menu">
           <li><%= link_to 'My Sitter Page', sitter_path(current_sitter.id) %></li>
           <li><%= link_to 'Booking Status', sitter_bookings_path(current_sitter.id) %></li>           
           <li><%= link_to 'Edit', edit_sitter_path(current_sitter.id) %></li>
          </ul>  
        </li>
      <li class="dropdown"><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
      <li class="dropdown"><%= link_to current_user.first_name, user_bookings_index_path(current_user.id) %></li>
      <span><%= link_to image_tag(current_user.avatar, class:"navbar-avatar"), user_bookings_index_path(current_user.id) %></span>
      <% else %>
        <li class="dropdown"><%= link_to '<i class="fa fa-sign-in" aria-hidden="true"></i>Login'.html_safe, new_session_path %></li>
        <li class="dropdown"><%= link_to '<i class="fa fa-user-plus" aria-hidden="true"></i>Sign Up'.html_safe, new_user_path %></li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>